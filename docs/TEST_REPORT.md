# 面板品牌自定义专项测试报告

## 测试信息

- 测试日期：2026-07-02
- 测试范围：品牌名称、Slogan 与面板图标自定义
- 前端地址：`http://127.0.0.1:5173`
- 后端地址：`http://127.0.0.1:8080/api/v1`
- 数据库：H2 内存测试库，已执行 V1 至 V3 迁移
- 测试方式：启动真实前后端服务后进行浏览器端操作与刷新验证

## 测试结果

| 用例 | 操作 | 实际结果 | 结论 |
| --- | --- | --- | --- |
| BRAND-01 | 将品牌名称改为“校园影像库”，Slogan 改为“记录校园每一刻”，保存并刷新 | 侧栏上方大字和下方小字均立即更新；刷新后两个文案保持不变 | 通过 |
| BRAND-02 | 选择系统内置“星标”图标，保存并刷新 | 侧栏图标立即变为 `star`；刷新后仍为星标 | 通过 |
| BRAND-03 | 上传 64 × 64、445 B 的 PNG 图片并刷新 | 接口返回 `iconType=custom`；侧栏成功加载图片，浏览器读取到自然宽度 64 px；设置页显示图片预览 | 通过 |

## 结论

本次品牌专项测试全部通过。管理员可分别修改上方品牌名称和下方 Slogan，也可在系统内置图标与上传图片之间切换；所有修改均能即时反映到侧栏，并能经后端持久化后在页面刷新时正确恢复。

自定义图片在前后端均限制为 PNG、JPEG、最大 512 KB、最大 1024 × 1024 像素。配置字段和界面文案均使用正确拼写 `slogan` / `Slogan`。
